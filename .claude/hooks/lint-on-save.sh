#!/usr/bin/env bash
# Placeholder — no linter configured for single-file vanilla HTML project.
# If node is available, could run: npx --yes html-validate index.html
echo "lint-on-save: no linter configured"
exit 0
